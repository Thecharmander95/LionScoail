%h1 Every User
.grid-x
  - @users.each do |user|
    .medium-4.cell
      .callout
        = user.username
        %br/
        Bio = #{user.body}
        %br/
        = link_to "#{user.username}'s Profile", user_path(user)
        %br/
