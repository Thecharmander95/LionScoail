%h1 Users
.callout
  - @users = User.all
  - @users.each do |u|
    = u.username
    %br/
    = link_to "Delete",
      user_path(u),
      method: :delete,
      data: { confirm: "Really delete this user? What did they do." }
    %br/
